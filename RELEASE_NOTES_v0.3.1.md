## What's new

Version bump: 0.3.0 → 0.3.1

### Changes
- Fixed OpenFOAM Docker image build (pip upgrade before editable install)
- Docker images now published to both Docker Hub and GitHub Container Registry (ghcr.io)
- CI: ghcr.io publishing with `packages: write` permission
- PyPI badges switched to badgen.net (no error caching)
- DOI badge now explicitly shows concept DOI `10.5281/zenodo.21462233`
- Docker badges added to README
