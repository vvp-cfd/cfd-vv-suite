FoamFile { version 2.0; format ascii; class volScalarField; object e; }
dimensions [0 2 -2 0 0 0 0]; internalField uniform 2.0;
boundaryField {
    left  { type zeroGradient; }
    right { type zeroGradient; }
    walls { type empty; }
}
