FoamFile { version 2.0; format ascii; class dictionary; object controlDict; }
application foamRun;
allowSystemOperations true;
startFrom startTime; startTime 0; stopAt endTime; endTime 0.2;
deltaT 5e-6; writeControl adjustableRunTime; writeInterval 0.05;
adjustTimeStep no; maxCo 0.1; maxDeltaT 1;
functions {
    sampleDict {
        type sets;
        libs ("libsampling.so");
        writeControl outputTime;
        interpolationScheme cellPoint;
        setFormat csv;
        fields (rho U p);
        sets (
            line {
                type lineUniform; axis x;
                start (0 0.005 0.005); end (1 0.005 0.005); nPoints 400;
            }
        );
    }
}
