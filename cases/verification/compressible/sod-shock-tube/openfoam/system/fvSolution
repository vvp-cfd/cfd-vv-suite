FoamFile { version 2.0; format ascii; class dictionary; object fvSolution; }
solvers {
    "rho.*" { solver diagonal; }
    "p.*"   { solver diagonal; }
    "U.*"   { solver smoothSolver; smoother symGaussSeidel; tolerance 1e-06; }
    "e.*"   { solver smoothSolver; smoother symGaussSeidel; tolerance 1e-06; }
}
PIMPLE {
    nOuterCorrectors 2;
}
